import React from 'react';
import QueueTable from '../components/QueueTable';

const Else: React.FC = () => {
  return (
    <div className="else">
      <h1>Queue</h1>
      <QueueTable />
    </div>
  );
};

export default Else;
