import React from 'react';
import QueueTable from '../components/QueueTable';

const Else: React.FC = () => {
  return (
    <div className="else">
      <h1>This is the Else Page</h1>
      <QueueTable />
    </div>
  );
};

export default Else;
